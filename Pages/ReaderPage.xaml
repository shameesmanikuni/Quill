<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Quill.Pages.ReaderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quill.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#131313">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#1B1B1C" Padding="12,0,12,0">
            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <Button Click="BackButton_Click" 
                        Background="Transparent" 
                        BorderThickness="0" 
                        Width="40" Height="40" CornerRadius="20">
                    <FontIcon Glyph="&#xE72B;" FontSize="16" Foreground="White"/>
                </Button>
                <TextBlock x:Name="BookTitleLabel" 
                           Foreground="White" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="8">
                <TextBlock x:Name="PageIndicator" Foreground="#889299" FontSize="13" VerticalAlignment="Center" Margin="0,0,12,0"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" 
                      x:Name="ReaderScroll" 
                      VerticalScrollMode="Enabled" 
                      HorizontalScrollMode="Disabled" 
                      Background="#0E0E0E">
            <ItemsRepeater x:Name="PageRepeater">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="20"/>
                </ItemsRepeater.Layout>
                <DataTemplate x:DataType="ImageSource">
                    <Image Source="{x:Bind}" 
                           Stretch="Uniform" 
                           HorizontalAlignment="Center" 
                           Margin="0,20,0,20"
                           MaxWidth="1000"/>
                </DataTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>